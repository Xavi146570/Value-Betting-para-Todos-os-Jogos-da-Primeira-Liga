[variables]
PYTHON_VERSION = "3.11"
PIP_DISABLE_PIP_VERSION_CHECK = "1"

[phases.install]
cmds = [
  "python3.11 -m pip install --upgrade pip setuptools wheel",
  "python3.11 -m pip install -r requirements.txt"
]

[start]
cmd = "python3.11 -m uvicorn app:app --host 0.0.0.0 --port $PORT"
